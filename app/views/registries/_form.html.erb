<%= form_for @registry, :html => { :class => 'form-horizontal' } do |f| %>
  <div class="control-group">
    <%= f.label :user_number, :class => 'control-label' %>
    <div class="controls">
      <%= f.number_field :user_number, :class => 'number_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :actividad_id, :class => 'control-label' %>
    <div class="controls">
      <%= f.number_field :actividad_id, :class => 'number_field' %>
    </div>
  </div>

  <div class="form-actions">
    <%= f.submit nil, :class => 'btn btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                registries_path, :class => 'btn' %>
  </div>
<% end %>
