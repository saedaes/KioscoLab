<%= form_for(@especial_news) do |f| %>
  <% if @especial_news.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@especial_news.errors.count, "error") %> prohibited this especial_news from being saved:</h2>

      <ul>
      <% @especial_news.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :publicacion_id %><br />
    <%= f.number_field :publicacion_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
